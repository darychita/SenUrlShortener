import React from 'react';
import InfoPage from '../InfoPage';

const RegistrationSuccessful = ({ message }) => {
    return (
        <InfoPage message={message} />
    );
};  

export default RegistrationSuccessful;
